[package]
name = "rustasea-macros"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Procedural macros for RustaSea — route, middleware, authorize, tries, backoff, timeout, usage, help, hidden"

[lib]
proc-macro = true

[dependencies]
syn = { workspace = true }
quote = { workspace = true }
proc-macro2 = { workspace = true }
