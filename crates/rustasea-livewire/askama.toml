# askama template discovery for this crate's own build/tests.
# Applications set the same root so `#[template(path = "...")]` resolves under
# `resources/views/` (ADR-0002 §D5, blueprint §6).
[general]
dirs = ["resources/views"]
