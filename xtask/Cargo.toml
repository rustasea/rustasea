[package]
name = "xtask"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "RustaSea cargo-xtask — CI gates: fmt, clippy, cycles (M5)."

[[bin]]
name = "xtask"
path = "src/main.rs"

[dependencies]
